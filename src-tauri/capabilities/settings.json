{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Settings window — autostart toggle + general settings",
  "windows": ["settings"],
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-minimize"
  ]
}
